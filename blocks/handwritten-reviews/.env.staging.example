# Staging credentials for upload-to-media.sh --target staging
#
# 1. Copy this file:
#      cp .env.staging.example .env.staging
#
# 2. Fill in the values below.
#
# 3. Generate an Application Password in WP Admin:
#      Users → Profile → Application Passwords → Add New
#    Copy the generated password (spaces included) into STAGING_APP_PASS.
#
# 4. Source before running the script:
#      source blocks/handwritten-reviews/.env.staging
#      bash upload-to-media.sh --target staging --year 2019 --month 05

export STAGING_URL="https://newsite-staging.hastingsandhastings.com"
export STAGING_USER="mbndev"
export STAGING_APP_PASS="Lhcu dKsI P9PF nVg2 zW2w aw21"
